export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  apiKey: 'www.Jefferson.qweqwe'
};

export interface Environment {
  production: boolean;
  apiUrl: string;
  apiKey: string;
}